export interface TableConfig {
  rowsPerPageOptions: number[];
  // Có thể thêm các cấu hình khác ở đây sau này
}

export const TABLE_CONFIG: TableConfig = {
  rowsPerPageOptions: [10, 20, 50, 100, 200],
  // Có thể thêm các cấu hình khác ở đây sau này
}; 